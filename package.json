{
  "name": "transmate-api",
  "version": "1.0.1",
  "dependencies": {
    "@redocly/openapi-cli": "^1.0.0-beta.29",
    "snippet-enricher-cli": "0.0.8"
  },
  "private": true,
  "scripts": {
    "start": "openapi preview-docs",
    "build": "openapi bundle -o dist",
    "test": "openapi lint",
    "add-scripts": "./node_modules/.bin/snippet-enricher-cli --input=dist.json > dist-with-examples.json",
    "all" : "npm run build && npm run add-scripts"
  }
}
